diff a/nannou_wgpu/src/bind_group_builder.rs b/nannou_wgpu/src/bind_group_builder.rs	(rejected hunks)
@@ -61,10 +61,9 @@ impl LayoutBuilder {
     /// Add a sampler binding to the layout.
     pub fn sampler(self, visibility: wgpu::ShaderStages, filtering: bool) -> Self {
         let comparison = false;
-        let ty = if filtering {
-            wgpu::BindingType::Sampler(wgpu_upstream::SamplerBindingType::Filtering)
-        } else {
-            wgpu::BindingType::Sampler(wgpu_upstream::SamplerBindingType::Comparison)
+        let ty = wgpu::BindingType::Sampler {
+            filtering,
+            comparison,
         };
         self.binding(visibility, ty)
     }
@@ -72,15 +71,10 @@ impl LayoutBuilder {
     /// Add a sampler binding to the layout.
     pub fn comparison_sampler(self, visibility: wgpu::ShaderStages, filtering: bool) -> Self {
         let comparison = true;
-        let ty = if filtering {
-            wgpu::BindingType::Sampler(wgpu_upstream::SamplerBindingType::Filtering)
-        } else {
-            wgpu::BindingType::Sampler(wgpu_upstream::SamplerBindingType::Comparison)
+        let ty = wgpu::BindingType::Sampler {
+            filtering,
+            comparison,
         };
-        // let ty = wgpu::BindingType::Sampler {
-        //     filtering,
-        //     comparison,
-        // };
         self.binding(visibility, ty)
     }
 
